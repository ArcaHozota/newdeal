// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"newdeal/ent/hymn"
	"newdeal/ent/hymnswork"
	"newdeal/ent/predicate"
	"newdeal/ent/student"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// HymnQuery is the builder for querying Hymn entities.
type HymnQuery struct {
	config
	ctx           *QueryContext
	order         []hymn.OrderOption
	inters        []Interceptor
	predicates    []predicate.Hymn
	withUpdatedBy *StudentQuery
	withWork      *HymnsWorkQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the HymnQuery builder.
func (hq *HymnQuery) Where(ps ...predicate.Hymn) *HymnQuery {
	hq.predicates = append(hq.predicates, ps...)
	return hq
}

// Limit the number of records to be returned by this query.
func (hq *HymnQuery) Limit(limit int) *HymnQuery {
	hq.ctx.Limit = &limit
	return hq
}

// Offset to start from.
func (hq *HymnQuery) Offset(offset int) *HymnQuery {
	hq.ctx.Offset = &offset
	return hq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (hq *HymnQuery) Unique(unique bool) *HymnQuery {
	hq.ctx.Unique = &unique
	return hq
}

// Order specifies how the records should be ordered.
func (hq *HymnQuery) Order(o ...hymn.OrderOption) *HymnQuery {
	hq.order = append(hq.order, o...)
	return hq
}

// QueryUpdatedBy chains the current query on the "updated_by" edge.
func (hq *HymnQuery) QueryUpdatedBy() *StudentQuery {
	query := (&StudentClient{config: hq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hymn.Table, hymn.FieldID, selector),
			sqlgraph.To(student.Table, student.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, hymn.UpdatedByTable, hymn.UpdatedByColumn),
		)
		fromU = sqlgraph.SetNeighbors(hq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryWork chains the current query on the "work" edge.
func (hq *HymnQuery) QueryWork() *HymnsWorkQuery {
	query := (&HymnsWorkClient{config: hq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := hq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := hq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(hymn.Table, hymn.FieldID, selector),
			sqlgraph.To(hymnswork.Table, hymnswork.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, hymn.WorkTable, hymn.WorkColumn),
		)
		fromU = sqlgraph.SetNeighbors(hq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Hymn entity from the query.
// Returns a *NotFoundError when no Hymn was found.
func (hq *HymnQuery) First(ctx context.Context) (*Hymn, error) {
	nodes, err := hq.Limit(1).All(setContextOp(ctx, hq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{hymn.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (hq *HymnQuery) FirstX(ctx context.Context) *Hymn {
	node, err := hq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Hymn ID from the query.
// Returns a *NotFoundError when no Hymn ID was found.
func (hq *HymnQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = hq.Limit(1).IDs(setContextOp(ctx, hq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{hymn.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (hq *HymnQuery) FirstIDX(ctx context.Context) int64 {
	id, err := hq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Hymn entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Hymn entity is found.
// Returns a *NotFoundError when no Hymn entities are found.
func (hq *HymnQuery) Only(ctx context.Context) (*Hymn, error) {
	nodes, err := hq.Limit(2).All(setContextOp(ctx, hq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{hymn.Label}
	default:
		return nil, &NotSingularError{hymn.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (hq *HymnQuery) OnlyX(ctx context.Context) *Hymn {
	node, err := hq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Hymn ID in the query.
// Returns a *NotSingularError when more than one Hymn ID is found.
// Returns a *NotFoundError when no entities are found.
func (hq *HymnQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = hq.Limit(2).IDs(setContextOp(ctx, hq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{hymn.Label}
	default:
		err = &NotSingularError{hymn.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (hq *HymnQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := hq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Hymns.
func (hq *HymnQuery) All(ctx context.Context) ([]*Hymn, error) {
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryAll)
	if err := hq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Hymn, *HymnQuery]()
	return withInterceptors[[]*Hymn](ctx, hq, qr, hq.inters)
}

// AllX is like All, but panics if an error occurs.
func (hq *HymnQuery) AllX(ctx context.Context) []*Hymn {
	nodes, err := hq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Hymn IDs.
func (hq *HymnQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if hq.ctx.Unique == nil && hq.path != nil {
		hq.Unique(true)
	}
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryIDs)
	if err = hq.Select(hymn.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (hq *HymnQuery) IDsX(ctx context.Context) []int64 {
	ids, err := hq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (hq *HymnQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryCount)
	if err := hq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, hq, querierCount[*HymnQuery](), hq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (hq *HymnQuery) CountX(ctx context.Context) int {
	count, err := hq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (hq *HymnQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, hq.ctx, ent.OpQueryExist)
	switch _, err := hq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (hq *HymnQuery) ExistX(ctx context.Context) bool {
	exist, err := hq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the HymnQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (hq *HymnQuery) Clone() *HymnQuery {
	if hq == nil {
		return nil
	}
	return &HymnQuery{
		config:        hq.config,
		ctx:           hq.ctx.Clone(),
		order:         append([]hymn.OrderOption{}, hq.order...),
		inters:        append([]Interceptor{}, hq.inters...),
		predicates:    append([]predicate.Hymn{}, hq.predicates...),
		withUpdatedBy: hq.withUpdatedBy.Clone(),
		withWork:      hq.withWork.Clone(),
		// clone intermediate query.
		sql:  hq.sql.Clone(),
		path: hq.path,
	}
}

// WithUpdatedBy tells the query-builder to eager-load the nodes that are connected to
// the "updated_by" edge. The optional arguments are used to configure the query builder of the edge.
func (hq *HymnQuery) WithUpdatedBy(opts ...func(*StudentQuery)) *HymnQuery {
	query := (&StudentClient{config: hq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hq.withUpdatedBy = query
	return hq
}

// WithWork tells the query-builder to eager-load the nodes that are connected to
// the "work" edge. The optional arguments are used to configure the query builder of the edge.
func (hq *HymnQuery) WithWork(opts ...func(*HymnsWorkQuery)) *HymnQuery {
	query := (&HymnsWorkClient{config: hq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	hq.withWork = query
	return hq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		NameJp string `json:"name_jp,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Hymn.Query().
//		GroupBy(hymn.FieldNameJp).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (hq *HymnQuery) GroupBy(field string, fields ...string) *HymnGroupBy {
	hq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &HymnGroupBy{build: hq}
	grbuild.flds = &hq.ctx.Fields
	grbuild.label = hymn.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		NameJp string `json:"name_jp,omitempty"`
//	}
//
//	client.Hymn.Query().
//		Select(hymn.FieldNameJp).
//		Scan(ctx, &v)
func (hq *HymnQuery) Select(fields ...string) *HymnSelect {
	hq.ctx.Fields = append(hq.ctx.Fields, fields...)
	sbuild := &HymnSelect{HymnQuery: hq}
	sbuild.label = hymn.Label
	sbuild.flds, sbuild.scan = &hq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a HymnSelect configured with the given aggregations.
func (hq *HymnQuery) Aggregate(fns ...AggregateFunc) *HymnSelect {
	return hq.Select().Aggregate(fns...)
}

func (hq *HymnQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range hq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, hq); err != nil {
				return err
			}
		}
	}
	for _, f := range hq.ctx.Fields {
		if !hymn.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if hq.path != nil {
		prev, err := hq.path(ctx)
		if err != nil {
			return err
		}
		hq.sql = prev
	}
	return nil
}

func (hq *HymnQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Hymn, error) {
	var (
		nodes       = []*Hymn{}
		_spec       = hq.querySpec()
		loadedTypes = [2]bool{
			hq.withUpdatedBy != nil,
			hq.withWork != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Hymn).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Hymn{config: hq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, hq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := hq.withUpdatedBy; query != nil {
		if err := hq.loadUpdatedBy(ctx, query, nodes, nil,
			func(n *Hymn, e *Student) { n.Edges.UpdatedBy = e }); err != nil {
			return nil, err
		}
	}
	if query := hq.withWork; query != nil {
		if err := hq.loadWork(ctx, query, nodes, nil,
			func(n *Hymn, e *HymnsWork) { n.Edges.Work = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (hq *HymnQuery) loadUpdatedBy(ctx context.Context, query *StudentQuery, nodes []*Hymn, init func(*Hymn), assign func(*Hymn, *Student)) error {
	ids := make([]int64, 0, len(nodes))
	nodeids := make(map[int64][]*Hymn)
	for i := range nodes {
		fk := nodes[i].UpdatedUser
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(student.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "updated_user" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (hq *HymnQuery) loadWork(ctx context.Context, query *HymnsWorkQuery, nodes []*Hymn, init func(*Hymn), assign func(*Hymn, *HymnsWork)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*Hymn)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(hymnswork.FieldWorkID)
	}
	query.Where(predicate.HymnsWork(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(hymn.WorkColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.WorkID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "work_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (hq *HymnQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := hq.querySpec()
	_spec.Node.Columns = hq.ctx.Fields
	if len(hq.ctx.Fields) > 0 {
		_spec.Unique = hq.ctx.Unique != nil && *hq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, hq.driver, _spec)
}

func (hq *HymnQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(hymn.Table, hymn.Columns, sqlgraph.NewFieldSpec(hymn.FieldID, field.TypeInt64))
	_spec.From = hq.sql
	if unique := hq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if hq.path != nil {
		_spec.Unique = true
	}
	if fields := hq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, hymn.FieldID)
		for i := range fields {
			if fields[i] != hymn.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if hq.withUpdatedBy != nil {
			_spec.Node.AddColumnOnce(hymn.FieldUpdatedUser)
		}
	}
	if ps := hq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := hq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := hq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := hq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (hq *HymnQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(hq.driver.Dialect())
	t1 := builder.Table(hymn.Table)
	columns := hq.ctx.Fields
	if len(columns) == 0 {
		columns = hymn.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if hq.sql != nil {
		selector = hq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if hq.ctx.Unique != nil && *hq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range hq.predicates {
		p(selector)
	}
	for _, p := range hq.order {
		p(selector)
	}
	if offset := hq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := hq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// HymnGroupBy is the group-by builder for Hymn entities.
type HymnGroupBy struct {
	selector
	build *HymnQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (hgb *HymnGroupBy) Aggregate(fns ...AggregateFunc) *HymnGroupBy {
	hgb.fns = append(hgb.fns, fns...)
	return hgb
}

// Scan applies the selector query and scans the result into the given value.
func (hgb *HymnGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hgb.build.ctx, ent.OpQueryGroupBy)
	if err := hgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HymnQuery, *HymnGroupBy](ctx, hgb.build, hgb, hgb.build.inters, v)
}

func (hgb *HymnGroupBy) sqlScan(ctx context.Context, root *HymnQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(hgb.fns))
	for _, fn := range hgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*hgb.flds)+len(hgb.fns))
		for _, f := range *hgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*hgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// HymnSelect is the builder for selecting fields of Hymn entities.
type HymnSelect struct {
	*HymnQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (hs *HymnSelect) Aggregate(fns ...AggregateFunc) *HymnSelect {
	hs.fns = append(hs.fns, fns...)
	return hs
}

// Scan applies the selector query and scans the result into the given value.
func (hs *HymnSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, hs.ctx, ent.OpQuerySelect)
	if err := hs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*HymnQuery, *HymnSelect](ctx, hs.HymnQuery, hs, hs.inters, v)
}

func (hs *HymnSelect) sqlScan(ctx context.Context, root *HymnQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(hs.fns))
	for _, fn := range hs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*hs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := hs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
